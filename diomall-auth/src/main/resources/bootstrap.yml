spring:
  application:
    name: diomall-auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: 8db42f00-48d2-45e7-a81c-d597ebf6d38a
        group: dev
        extension-configs:
          - dataId: misc.yml
            group: dev
            refresh: true
          - dataId: spring-cloud.yml
            group: dev
            refresh: true
          - dataId: cache.yml
            group: dev
            refresh: true
          - dataId: oauth.yml
            group: dev
            refresh: true